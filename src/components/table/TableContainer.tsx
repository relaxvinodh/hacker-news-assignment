import { styled } from '../../theme';

/**
 * Table container - used to enable proper styling in Tables, could be omitted for simple tables
 */
const TableContainer = styled.div``;

export default TableContainer;
